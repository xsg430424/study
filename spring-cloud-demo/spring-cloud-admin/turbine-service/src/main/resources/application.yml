server:
  port: 8769

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  security:
    enabled: false

spring:
  application:
    name: turbine-service

security.basic.enabled: false
turbine:
  aggregator:
    clusterConfig: default
  appConfig: admin-client-one,admin-client-two
  clusterNameExpression: new String("default")
